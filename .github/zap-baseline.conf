# 10020: Missing Anti-clickjacking Header
# Ignored because slide-spec generates static assets and does not control deployment response headers.
10020	IGNORE

# 10021: X-Content-Type-Options Header Missing
# Ignored because this is a web-server/header policy concern owned by the eventual host or CDN.
10021	IGNORE

# 10038: Content Security Policy Header Not Set
# Ignored because CSP is enforced by deployment headers, not by the generated static files alone.
10038	IGNORE

# 10049: Storable and Cacheable Content
# Ignored because caching policy is host-managed and not a meaningful app-layer vulnerability for this static site.
10049	IGNORE

# 10063: Permissions Policy Header Not Set
# Ignored because Permissions-Policy is configured at the host/header layer, outside the generator's control.
10063	IGNORE

# 10109: Modern Web Application
# Ignored because this is framework fingerprinting/information, not an actionable security issue for CI gating.
10109	IGNORE

# 90004: Cross-Origin-Embedder-Policy Header Missing or Invalid
# Ignored because COEP is a deployment header choice that the generated site cannot set by itself.
90004	IGNORE
