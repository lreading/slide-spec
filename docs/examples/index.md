# Examples

[tutorial-example](/examples/tutorial-example) — end-to-end from `init` through concrete YAML edits; kept in sync with the fixture under `docs/fixtures/reference-project/`.

[manual-data-example](/examples/manual-data-example) — no GitHub connector; you own `generated.yaml` (good for internal or non-GitHub sources).

Template screenshots are generated from that reference fixture so published images always match a real build.
