# Supply Chain

Release artifacts and CI outputs are the source of truth for binaries, checksums, and (when wired up) SBOMs. Point readers at GitHub **Releases** and the repository README badges for current gate status. SBOM location and format will follow once the release and Actions workflows publish them consistently.
