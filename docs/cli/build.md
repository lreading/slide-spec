# `build`

`build` emits a static site into `<project-root>/dist`.

```bash
npx @slide-spec/cli build ./my-slides
```

Output is plain HTML, CSS, and JS. No runtime server is required. The CLI uses its bundled app build; the project does not need a local `app/` checkout.
