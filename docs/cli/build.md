# `build`

`build` produces the static output for deployment.

## Behavior

- Uses the project root.
- Writes the built site to `dist/`.
- Does not require a backend.

## Example

```bash
npx @slide-spec/cli build ./my-slides
```
